<%= form_with(model: create_user, local: true) do |form| %>
  <% if create_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(create_user.errors.count, "error") %> prohibited this create_user from being saved:</h2>

      <ul>
        <% create_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
